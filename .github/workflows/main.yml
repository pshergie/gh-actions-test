on:
  # Trigger the workflow on push or pull request
  pull_request:
    branches:
      - main
      - master

jobs:
  comment:
    runs-on: ubuntu-latest
    steps:
      - uses: ./
        with:
          message: "Test message!"
